var log = function () {
    console.log("by");
};
// in typescript that is way we can  create a function
// if we have a single statement we can also exclude curly braces
var logs = function (messages) {
    console.log(messages);
};
logs('umar shaikh');
var drawPoint = function (point) {
    console.log(point);
};
drawPoint({
    x: 1,
    y: 2
});
